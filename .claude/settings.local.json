{
  "permissions": {
    "allow": [
      "Bash(mkdir -p ~/hackerrank_orchestrate)",
      "Bash(python3 -c \"import sys; sys.path.insert\\(0, 'code'\\); from agent import Agent; from retriever import Retriever; from classifier import check_escalation; from indexer import build_index; from prompts import SYSTEM_PROMPT; print\\('All imports OK'\\)\")",
      "Bash(python3:*)",
      "Bash(python:*)",
      "Bash(find . -type d -name __pycache__ -not -path './.venv/*' -exec rm -rf {} +)",
      "Bash(rm -f code.zip submissions.zip)",
      "Bash(rm -rf submissions/)",
      "Bash(git add:*)"
    ]
  }
}
